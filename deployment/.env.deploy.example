# Salin file ini menjadi deployment/.env.deploy (TANPA .example) lalu isi nilai
# asli. File deployment/.env.deploy TIDAK akan tertrack git (sudah masuk pola
# .env* di .gitignore root repo) — aman dari ke-commit ke git history.
#
# Setelah diisi, jalankan seperti biasa:
#   bash setup-vps.sh
# Script otomatis membaca file ini kalau ada.

DOMAIN=api.DOMAIN-ANDA.com
DB_PASS=GANTI_PASSWORD_KUAT
REPO_URL=git@github.com:cuttlefeesh/sistemhelpdesk.git
